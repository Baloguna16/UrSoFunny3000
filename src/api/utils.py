def check_content(status):
    """Checks to assure that status.text is content safe."""

    return True

def load_stored_resposne(status):
    """Building custom responses based on stored content."""

    response = "I like cheese."
    return response
